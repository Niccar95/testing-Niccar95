import { IMovie } from "../ts/models/Movie";

describe("This function should sort movies", () => {
  test("This should sort", () => {
    const movies: IMovie[] = [
      {
        Title: "yo",
        imdbID: "drfr",
        Type: "ffr",
        Poster: "dff",
        Year: "fff",
      },
    ];
  });
});
